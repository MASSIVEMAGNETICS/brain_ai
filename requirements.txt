numpy>=1.20.0

# Optional: GPU acceleration (install at least one for Phase 3 features)
# torch>=2.0.0  # Uncomment for PyTorch GPU support
# tensorflow>=2.12.0  # Uncomment for TensorFlow GPU support
